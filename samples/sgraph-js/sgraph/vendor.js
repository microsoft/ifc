// Copyright Microsoft Corporation.
// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception

function symbolic_for_vendor_sort(sort) {
    switch (sort) {
    default:
        console.error(`Bad sort: ${sort}`);
        return null;
    }
}